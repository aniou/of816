; Should we include the romloader 
.define romloader_as_word 1

; Should we instead put it in the FCode to install at startup?
.define romloader_at_init 0

; Should we replace default font, with ST-like one?
.define alt_font 1
